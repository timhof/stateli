
page[:flashdiv].hide

page[:transactions_pending_details_credit].replace_html :partial => 'pending_details', :collection => @transactions

if @transactions.length > 0
	page[:transactions_pending_credit].show
else
	page[:transactions_pending_credit].hide
end