<h1>New rule</h1>

<% form_for(@rule, :rule, :url => account_rule_path(@account)) do |f| %>
	  <%= f.error_messages %>
  <%= render :partial => 'edit_form', :locals => {:f => f} %>
  
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', account_rules_path(@account) %>
